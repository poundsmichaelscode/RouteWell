# Migration mirror

Prisma requires migrations beside the schema, so executable migrations live in `database/prisma/migrations/`. This directory is retained to match the enterprise repository layout and may hold reviewed SQL exports or DBA change records.
